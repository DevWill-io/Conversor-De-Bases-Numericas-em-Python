# Conversor de Bases Numéricas em Python

Conversor de Bases Numéricas 🔢

​Este é um script simples em Python que permite ao usuário converter um número inteiro para três bases diferentes: Binário, Octal e Hexadecimal.

​🚀 Como funciona

​O programa solicita um número inteiro ao usuário e oferece um menu de opções. Dependendo da escolha, ele utiliza as funções nativas do Python para realizar a conversão:

​bin(): Converte para binário.

​oct(): Converte para octal.

​hex(): Converte para hexadecimal.

​Nota: O script utiliza slicing [2:] para remover os prefixos identificadores (0b para binário, 0o para octal e 0x para hexadecimal) que o Python adiciona automaticamente.

​🛠️ Tecnologias Utilizadas

​Linguagem: Python 3

​Paradigma: Programação Estruturada
